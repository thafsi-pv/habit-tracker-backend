export declare function parseCalendarDate(dateStr: string): Date;
export declare function formatCalendarDate(date: Date): string;
export declare function todayInTimezone(timezone: string): string;
export declare function addDays(dateStr: string, days: number): string;
